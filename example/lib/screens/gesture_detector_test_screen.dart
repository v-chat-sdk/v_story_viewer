import 'package:flutter/material.dart';

/// Test screen for v_gesture_detector feature
class GestureDetectorTestScreen extends StatelessWidget {
  const GestureDetectorTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gesture Detector Test'),
      ),
      body: const Center(
        child: Text('Gesture Detector Test Screen - Coming Soon'),
      ),
    );
  }
}
