import 'package:flutter/material.dart';

/// Test screen for v_reply_system feature
class ReplySystemTestScreen extends StatelessWidget {
  const ReplySystemTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Reply System Test'),
      ),
      body: const Center(
        child: Text('Reply System Test Screen - Coming Soon'),
      ),
    );
  }
}
